{
  "name": "beautiful-backgrounds",
  "version": "0.0.1",
  "author": "Niekes",
  "description": "A helper library to help me build amazing web apps",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist/components dist/utils/ && rm -f dist/index.js dist/index.d.ts",
    "build": "npm run clean && npx tsc",
    "watch": "npm run clean && npx tsc --watch",
    "release": "npm run clean && npm run build && git push --follow-tags && npm publish"
  },
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "typescript": "5.3.2"
  }
}
